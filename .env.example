# .env.example
# ============================================================================
# Environment Variables Template für Studiennavigator
# ============================================================================
#
# SETUP:
# 1. Kopiere diese Datei: cp .env.example .env
# 2. Fülle deine echten Werte ein
# 3. WICHTIG: .env wird nicht in Git committed!
#

# ============================================================================
# FLASK CONFIGURATION
# ============================================================================
# Generiere einen sicheren Key mit:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-replace-with-64-character-hex-string

# ============================================================================
# E-MAIL CONFIGURATION (SMTP)
# ============================================================================
# Für Strato SMTP (kann bei anderem Provider angepasst werden)
MAIL_SERVER=smtp.strato.de
MAIL_PORT=465
MAIL_USE_SSL=True
MAIL_USE_TLS=False

# Deine E-Mail-Adresse für den Versand
MAIL_USERNAME=your-email@your-domain.org

# Dein E-Mail-Passwort (verwende Anführungszeichen bei Sonderzeichen!)
MAIL_PASSWORD="your-password-here"

# Absender-Name und E-Mail für ausgehende Mails
MAIL_DEFAULT_SENDER="Studiennavigator <noreply@your-domain.org>"

# ============================================================================
# DATABASE
# ============================================================================
# Pfad zur SQLite-Datenbank (relativ zum Projektroot)
DB_PATH=data/dashboard.db
